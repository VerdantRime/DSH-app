# DeepSeek工作台 使用说明

一个集成 **DeepSeek Harness 聊天 + AI 代码助手 + GitHub + 本地 IDE** 的 Windows 桌面应用。

---

## 一、系统要求

- Windows 10 / 11（64 位）
- **Node.js（LTS）**：聊天（harness）和 AI 助手都依赖它。首次使用会自动检测，若未安装请到 https://nodejs.org 下载 LTS 版一路「下一步」安装，装完重启本应用。
- （可选）Python / MinGW-w64(GCC) / JDK：需要「编译/运行」对应语言时才装。

---

## 二、安装

- **安装版**：双击 `DeepSeek工作台 Setup x.x.x.exe`，按提示安装。
- **便携版**：双击 `DeepSeek工作台-x.x.x-portable.exe` 直接运行，无需安装（数据仍保存在 `%APPDATA%`）。

> 首次打开会弹出「首次启动引导」，点「开始环境检查」即可看到 Node.js / 工具链 / AI 密钥 / GitHub 是否就绪，并给出安装步骤。

---

## 三、各功能说明

### 1. 聊天（harness）
- 内嵌 DeepSeek Harness 网页。**首次打开按页面提示填写你的 DeepSeek API Key**。
- 需要 Node.js。

### 2. 代码（IDE）
- 内置 Monaco 编辑器（VSCode 同款内核），支持多标签、自动保存（停止输入约 15 秒）。
- 顶部按钮：新建 / 打开文件 / 打开文件夹 / 保存 / 运行 / 编译。
- 运行/编译 C/C++、Python、Java 前会先保存；编译出错会在编辑器标红并跳转到第一个错误。
- 语言可手动切换（右上角「语言」下拉框）。

### 3. AI 助手（编辑器右侧）
- 选中代码后点「解释 / 找错 / 优化」，或直接在下方输入框提问（回车发送）。
- 答案会**逐字流式输出**，默认模型 `deepseek-v4-flash`。
- 「优化」给出代码后，点「应用到编辑器」可一键替换，`Ctrl+Z` 可撤回。
- 需要 Node.js + DeepSeek API Key。

### 4. GitHub
- 在「设置 → GitHub」粘贴你的 **Personal Access Token**：只读权限可浏览/下载，读写权限才能上传/新建。
- 支持：浏览仓库/文件/Issue/PR/Commit、上传多个文件或整个文件夹（一次 commit、保留目录结构、显示进度）、批量下载、克隆仓库。
- 提示：GitHub 有「推送保护」，若上传的文件里含疑似密钥/密码，会被拦截（请删除敏感内容再传）。

### 5. 设置
- 主题 / 二次元壁纸（可上传自定义图片）。
- IDE 工具链路径（留空自动检测）。
- 使用帮助（含环境自检）。
- 统计信息（见下）。

### 6. 统计信息
- 记录：编码时长（按文件/语言）、编译出错次数、运行次数、保存次数、AI 使用次数、harness 对话时长等。
- 挂机（超过 3 分钟无操作）、切到后台、最小化到托盘都不计时。
- 退出应用时会弹出「本次会话总结」。

---

## 四、常见问题（FAQ）

**Q：聊天/AI 助手打不开或一直没反应？**
A：大概率是没装 Node.js。到「设置 → 使用帮助 → 环境自检」确认，缺了就装 Node.js LTS 并重启应用。

**Q：AI 能打开但回答报错？**
A：到「聊天」面板确认已填写 DeepSeek API Key（且账户有额度）。

**Q：编译/运行报「未检测到」？**
A：没装对应工具链。到「设置 → IDE 工具链」手动指定 Python / g++ / javac 路径，或安装后加入 PATH。

**Q：GitHub 上传失败，提示 Secret/密钥检测？**
A：上传的文件里含疑似密钥（API Key/token/密码），GitHub 推送保护会拒绝。删除敏感内容后重试。

---

## 五、数据存放位置

- 应用配置/统计/凭据：`%APPDATA%\dsh-workdesk`
- DeepSeek Harness 配置：`C:\Users\你的用户名\.dsh`
