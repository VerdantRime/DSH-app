# 开发计划（可直接执行）· 依据 PRD.md v1

> 本计划是 PRD.md 的落地施工方案：给出技术选型、目录结构、数据模型、IPC 契约、分阶段任务（可勾选）、风险专项与验收映射。
> 目标平台：Windows 11（AMD64）。全部验收以 PRD.md §10 为准。

---

## 0. 实施总览

一条主线的 10 个阶段（每阶段可独立验收）：

    P0 脚手架       →  P1 窗口/布局骨架  →  P2 harness 进程管理  →  P3 内嵌聊天+控制台
    P4 数据持久化   →  P5 GitHub 只读    →  P6 设置+主题        →  P7 托盘+生命周期
    P8 打包        →  P9 全量验收

依赖关系：P2 是核心难点（先做 Spike 验证）；P3 依赖 P2；P5 依赖 P4 的加密存储；P7 依赖 P2 的生命周期。

---

## 1. 技术选型与依赖

| 项 | 选型 | 说明 |
|---|---|---|
| 运行时 | Node v24（已装） | 与当前一致 |
| 桌面壳 | Electron（最新稳定版） | 内嵌 Chromium，与 Chrome 同内核，兼容 harness 这个 Vite/Web 应用 |
| 开发/构建 | electron-vite（Vite + TypeScript） | 主进程/preload/渲染进程三段式，渲染进程带 HMR |
| 打包 | electron-builder | Windows 目标：NSIS 安装包 + portable 便携 exe |
| 渲染 UI | 原生 TS + 手写轻量 DOM | 不引框架，减少依赖面；CSS 变量做主题 |
| GitHub | @octokit/rest | 只读 REST，自动处理分页与鉴权 |
| 备份 | adm-zip | zip 打包/解包 |
| 加密 | Electron safeStorage | Windows 下用 DPAPI 加密 GitHub token |
| 其他 | TypeScript ^5、@types/node | — |

安装命令（进入实现阶段时执行）：

    npm init -y
    npm i -D electron electron-vite vite typescript electron-builder @types/node
    npm i @octokit/rest adm-zip

版本策略：全部用「安装时最新稳定版」，用 package-lock.json 锁定，避免漂移。

---

## 2. 项目结构

    deep seek app/
    ├─ PRD.md
    ├─ 开发计划.md
    ├─ package.json
    ├─ electron.vite.config.ts
    ├─ electron-builder.yml
    ├─ tsconfig.json / tsconfig.node.json / tsconfig.web.json
    └─ src/
       ├─ main/                     # 主进程
       │  ├─ index.ts               # 入口：app 生命周期、单实例锁、装配
       │  ├─ window.ts              # 主窗口创建、bounds 记忆
       │  ├─ tray.ts                # 系统托盘
       │  ├─ harness-manager.ts     # harness 启动/停止/复用/状态机/日志捕获
       │  ├─ github.ts              # GitHub 只读客户端
       │  ├─ store.ts               # config.json 读写（防抖落盘）
       │  ├─ credentials.ts         # safeStorage 加解密 token
       │  ├─ backup.ts              # 一键备份/恢复
       │  ├─ logger.ts              # harness.log 滚动轮转
       │  └─ ipc.ts                 # 注册全部 IPC handler
       ├─ preload/
       │  └─ index.ts               # contextBridge 暴露 window.api（类型化）
       ├─ renderer/
       │  ├─ index.html
       │  ├─ main.ts                # 渲染入口、面板路由、主题应用
       │  ├─ styles/base.css
       │  ├─ styles/theme.css       # 明暗主题 CSS 变量
       │  └─ panels/
       │     ├─ chat.ts             # 聊天面板：webview + 运行控制台
       │     ├─ github/             # 仓库列表 / 详情 / 子页
       │     └─ settings.ts         # 设置面板
       └─ shared/
          └─ types.ts               # 共享类型 + IPC 通道常量

---

## 3. 数据模型与 IPC 契约

### 3.1 config.json（位于 %APPDATA%/<AppName>/config.json）

    {
      "version": 1,
      "theme": "system",
      "launchBehavior": { "autoStartHarness": true },
      "sidebarCollapsed": false,
      "harness": { "port": 3080, "dataDir": "<os.homedir()>/.dsh" },
      "windowBounds": { "x": 0, "y": 0, "width": 1200, "height": 800, "maximized": false }
    }

### 3.2 github.json（%APPDATA%/<AppName>/github.json）

    {
      "tokenEncrypted": "<safeStorage 加密后的 base64>",
      "username": "登录账号名"
    }

### 3.3 harness.log 轮转

- harness.log / harness.1.log / harness.2.log / harness.3.log / harness.4.log
- 单个文件上限 1MB，超出滚动；内存中保留最近 2000 行供界面展示。

### 3.4 IPC 通道（invoke/handle + 事件）

| 通道 | 方向 | 说明 |
|---|---|---|
| harness:getStatus | renderer→main | 返回 HarnessStatus |
| harness:start / stop / restart | renderer→main | 幂等；复用态下 stop 返回 stoppedOwn:false |
| harness:getLogs | renderer→main | 返回日志行数组 |
| harness:status-changed | main→renderer(事件) | 状态机变化推送 |
| harness:log | main→renderer(事件) | 新增日志行推送 |
| github:setToken / verifyToken / clearToken / getStatus | renderer→main | token 管理 |
| github:listRepos / searchRepos / getRepo | renderer→main | 列表与详情 |
| github:getContents / listIssues / getIssue / listPulls / getPull / listCommits / getCommit | renderer→main | 只读子资源 |
| config:get / config:set | renderer→main | 读取/局部更新配置 |
| backup:create / backup:restore | renderer→main | 备份/恢复 |
| app:openExternal / app:quitReal / app:showWindow / app:getVersion | renderer→main | 系统级动作 |

### 3.5 harness 状态机

    idle ──start──▶ starting ──就绪(端口+HTTP200)──▶ running(来源=app)
    starting ──失败──▶ error ──start──▶ starting
    启动前检测端口已被占用 ──▶ reused(来源=外部，不接管停止)
    running/reused ──stop──▶ idle（仅 running 可被 app 停止；reused 禁用停止）

---

## 4. 分阶段实施计划

### Phase 0 · 脚手架与环境准备

- [ ] 项目根初始化 npm 包、安装 §1 依赖
- [ ] 用 electron-vite 脚手架生成 main/preload/renderer 三段结构（目录名填 .，模板选 vanilla-ts）
- [ ] 配好 tsconfig（node 端与 web 端分离）、electron.vite.config.ts
- [ ] 配置 electron-builder：appId、productName、win 目标 [nsis, portable]、占位图标
- [ ] 冒烟：npm run dev 能弹出空白 Electron 窗口且无报错

**产出**：可运行的 dev 骨架。**验收**：dev 窗口弹出、主/preload/renderer 三段正常通信。

### Phase 1 · 窗口与布局骨架

- [ ] window.ts：原生标题栏、默认 1200×800、autoHideMenuBar、记住 bounds
- [ ] 渲染层静态布局：侧边栏（聊天/GitHub/设置 + 底部状态灯/版本）+ 内容区 + 底部状态栏
- [ ] 面板路由（chat/github/settings 切换）+ 侧边栏折叠
- [ ] store.ts：config.json 读写（防抖落盘），接入主题/折叠/bounds 持久化
- [ ] tray.ts 占位：菜单「显示主窗口 / 退出」
- [ ] 关闭窗口 = 隐藏到托盘；托盘「退出」= 真正退出

**验收**：三面板可切换；折叠状态重启后保持；关闭进托盘、托盘退出真正退出。

### Phase 2 · harness 进程管理（核心，先 Spike）

- [ ] **Spike**：写独立 Node 脚本，用 child_process 以 npx.cmd 启动 harness，验证：
  - 最小环境变量（DSH_HOME 显式设为 ~/.dsh，其余沿用 harness 默认）
  - 端口 3080 起来、HTTP 健康检查通过
  - stdout/stderr 可被逐行捕获
  - 结论固化为「启动参数 + env」常量，写进 harness-manager.ts 注释
- [ ] harness-manager.ts：
  - spawn（Windows 下用 npx.cmd + shell:true）、记录 ownPid、启动时间
  - 状态机实现（idle/starting/running/reused/error）
  - 端口复用检测 = 端口连通 + GET http://127.0.0.1:3080 返回 200
  - 日志逐行捕获 + 内存环形缓冲 + 落盘轮转
  - stop 用 taskkill /T /F 杀进程树；restart = stop+start
- [ ] ipc.ts 注册 harness:* 通道与事件

**验收**：可启动/停止/重启；日志实时可见；复用检测正确；退出无残留进程。

### Phase 3 · 聊天面板内嵌 + 运行控制台

- [ ] 主窗口 webPreferences 开 webviewTag:true
- [ ] 聊天面板放 webview 加载 http://127.0.0.1:3080，使用持久分区 partition=persist:harness
- [ ] webview 安全加固：阻止 will-navigate、new-window 交给系统浏览器、禁弹窗、nodeIntegration:false
- [ ] 运行控制台（**不换皮**，只展示 harness 网页没有的东西）：
  - 状态 chip（未启动/启动中/运行中/复用外部/出错）+ PID + 运行时长 + 端口 + 来源
  - 按钮：启动 / 停止 / 重启 / 在浏览器打开 / 复制地址
  - 可折叠「后端日志」抽屉：展示捕获的 stdout/stderr
  - 复用外部 harness 时「停止」禁用并提示「由外部启动，不接管停止」
- [ ] 订阅 harness:status-changed / harness:log 实时刷新

**验收**：聊天界面内嵌正常、能对话；控制台能看到 harness UI 看不到的后端日志；复用提示正确。

### Phase 4 · 数据持久化 + 备份恢复

- [ ] credentials.ts：safeStorage 加密/解密 GitHub token
- [ ] logger.ts：harness.log 滚动轮转（5 份 × 1MB）
- [ ] backup.ts：adm-zip 打包「app 数据目录 + ~/.dsh」到指定位置（含时间戳）；恢复前先自动备份当前
- [ ] 设置页预留「备份 / 恢复」入口，接相应通道

**验收**：重启数据不丢；备份 zip 生成；恢复后设置与聊天历史还原。

### Phase 5 · GitHub 只读集成

- [ ] github.ts：@octokit/rest + token；只读端点封装（见 §3.4）
- [ ] token 管理：setToken/verifyToken/clearToken/getStatus
- [ ] 仓库列表（我的/星标）、搜索
- [ ] 仓库详情 + 子标签：文件树与文件内容（高亮）、Issues、PR、Commits、README
- [ ] GitHub 面板渲染：未登录态→跳设置；列表页；详情页；面包屑 + 返回
- [ ] 全程不调用任何写接口（代码评审项）

**验收**：PRD §10 第 7 条——能看仓库/文件/issue/PR/commit/搜索，且无写操作。

### Phase 6 · 设置面板 + 主题

- [ ] 通用：主题（浅色/深色/跟随系统）、启动行为（自动启动 harness）、窗口偏好
- [ ] GitHub：token 输入/验证/清除 + 当前账号展示 + 只读权限说明
- [ ] Harness：端口、DSH_HOME 只读展示、启停重启、日志查看
- [ ] 关于：版本号、数据目录位置说明
- [ ] 主题：CSS 变量 + nativeTheme（light/dark/system），只作用于 app 自绘部分，不碰 harness 内嵌界面

**验收**：主题即时生效且持久；各设置持久。

### Phase 7 · 托盘 + 生命周期完善

- [ ] 托盘菜单：显示主窗口 / 直达聊天·GitHub·设置 / 退出
- [ ] 单实例锁（app.requestSingleInstanceLock），防止开两个实例抢端口
- [ ] 退出清理：只杀 app 启动的 harness（ownPid），复用外部的不动
- [ ] 关闭=隐藏到托盘（非退出）；托盘「退出」才停止 harness 并退出

**验收**：PRD §10 第 3 条。

### Phase 8 · 打包

- [ ] electron-builder 产出 NSIS 安装包 + portable 便携 exe
- [ ] 配置应用图标、productName
- [ ] 对安装版与便携版分别做冷启动冒烟

**验收**：双击 portable exe 可启动、全程无需 PowerShell/浏览器。

### Phase 9 · 全量验收

- [ ] 逐条执行 PRD §10 的 11 项验收标准，形成验收清单并记录通过/失败
- [ ] 修复发现的问题，复测通过后结束 v1

---

## 5. 「无 bug」专项（关键细节清单）

1. **Windows 启动 npx**：npx 是 .cmd，必须用 npx.cmd 且 shell:true 启动，否则 spawn ENOENT。
2. **杀进程树**：npx 会派生子 node 进程，停止用 taskkill /PID <pid> /T /F，否则残留。
3. **复用不接管**：外部已运行的 harness（来源=reused）不提供停止/重启，避免误杀用户当前会话。
4. **端口检测**：不能只 ping 端口，需「端口连通 + HTTP 200」双重确认，避免误判。
5. **DSH_HOME 显式传递**：spawn 时显式设 DSH_HOME=~/.dsh，确保配置/聊天历史沿用、不丢。
6. **webview 隔离**：持久分区 persist:harness 保留其 localStorage；禁 will-navigate/new-window，外链走系统浏览器。
7. **加密边界**：safeStorage(DPAPI) 与当前 Windows 用户绑定；换用户/换机需重新输入 token（在 UI 提示）。
8. **退出顺序**：先停 harness（仅 ownPid）→ 再写盘落 config → 再退出，防丢配置。
9. **单实例**：requestSingleInstanceLock 防重复进程抢 3080。
10. **日志不刷屏**：环形缓冲 + 轮转落盘，渲染端只保留尾部，避免内存/界面卡顿。

---

## 6. 验收映射（PRD §10 → 阶段）

| PRD 验收项 | 主要阶段 |
|---|---|
| 1 一键启动 | P2 + P3 + P8 |
| 2 无重复进程 | P2 |
| 3 关闭/托盘 | P1 + P7 |
| 4 错误可恢复 | P2 + P3 |
| 5 设置持久 | P1 + P4 + P6 |
| 6 聊天历史持久 | P2(DSH_HOME) + P4(备份) |
| 7 GitHub 只读 | P5 |
| 8 主题切换 | P6 |
| 9 后端日志可见 | P2 + P3 |
| 10 备份/恢复 | P4 |
| 11 内嵌无 bug | P3 + P5(webview 加固) |

---

## 7. 里程碑与提交节奏

- M1（P0–P1）：空壳可跑 + 布局导航
- M2（P2–P3）：内嵌 harness 可用 + 控制台（核心价值打通）
- M3（P4–P6）：持久化 + GitHub 只读 + 设置主题
- M4（P7–P8）：托盘生命周期 + 打包交付
- M5（P9）：验收通过

每个里程碑结束做一次手动回归，再进入下一里程碑。