# 工作 App（DeepSeek Harness 桌面工作台）· 产品需求文档（PRD）

- 版本：v1.0
- 状态：已与用户逐项确认
- 用途：本文件是后续设计与开发的唯一依据
- 平台：Windows 11（AMD64）

---

## 1. 产品目标和使用场景

### 1.1 产品目标

做一个**仅供本人使用的 Windows 桌面工作 app**，实现：

1. 把 DeepSeek Harness **无缝装进一个桌面窗口**，双击即用，**省掉「打开 PowerShell → 输入 npx @deepseek-ai/dsh web → 再开浏览器」的繁琐步骤**。
2. 集成 GitHub **只读**访问：浏览仓库、文件、issue、PR、commit、搜索。
3. 保证应用设置、聊天历史、GitHub 凭据、harness 配置**长期持久保存，绝不因重启/更新丢失**。
4. 布局合理（左侧边栏 + 右侧内容区），并确保内嵌 harness **无 bug**。

### 1.2 使用场景

| 场景 | 用户动作 | 期望结果 |
|---|---|---|
| 快速对话 | 双击 app → 默认进入聊天 | 直接开始和 DeepSeek 对话 |
| 查 GitHub | 切到 GitHub 面板 | 浏览仓库/文件/issue/PR/commit |
| 改设置 | 进入设置面板 | 改主题、GitHub token、harness 端口等 |
| 后台常驻 | 点窗口「关闭」 | 隐藏到托盘，随时从托盘唤起 |

---

## 2. 产品的可行性和可实现性

### 2.1 技术可行性

- **桌面壳**：Electron（内嵌 Chromium，与 Chrome 同内核）。技术成熟，能稳定加载 harness 这个 Vite/Web 应用。
- **harness 管理**：用 child_process 后台运行 npx @deepseek-ai/dsh web，并捕获其 stdout/stderr 作为「后端日志」。harness 当前已能独立运行，方案已验证可行。
- **GitHub 只读**：官方 REST API + fine-grained Personal Access Token（只读），成熟稳定。
- **数据持久化**：harness 数据已稳定在 C:\Users\Mao\.dsh；app 自身设置用 Electron userData（%APPDATA%）。

### 2.2 环境现状（已检测）

- Windows 11 家庭版（AMD64）；Node v24.18.0；npm 11.16.0；Git 2.54.0。
- pnpm 未安装（仅修改 harness 源码才需要，v1 不需要）。
- harness 数据根目录 DSH_HOME = C:\Users\Mao\.dsh，内含：settings.yaml、.credentials.yaml、sessions/、profiles/、storages/、.anonymous-user-id。
- harness Web 地址 DSH_WEB_URL = http://127.0.0.1:3080。
- npx 缓存目录 ...\_npx\1e7f6d9597241db0 只放代码、不放数据（已确认数据不会因清缓存丢失）。

### 2.3 主要风险与对策

| 风险 | 对策 |
|---|---|
| 3080 端口被已有 harness 占用 | 启动前检测端口；已占用则复用，不重复启动 |
| 退出时残留 harness 进程 | 记录 app 启动的 PID，退出时精准停止 |
| npx 首次冷启动慢 | 状态条显示「启动中」，不阻塞 UI |
| 内嵌页面白屏 / websocket 异常 | 同内核 Chromium 兼容性高；失败提供「重试 / 日志 / 浏览器打开」兜底 |
| 数据丢失 | 数据只落在 ~\.dsh 与 %APPDATA%，并提供一键备份/恢复 |

---

## 3. 整体页面框架和导航结构

### 3.1 窗口骨架

\`\`\`
┌────────────────────────────────────────────────┐
│  标题栏（app 名 + 窗口控制按钮，原生标题栏）        │
├──────────┬─────────────────────────────────────┤
│          │                                     │
│  侧边栏   │           内容区                     │
│ (可折叠)  │   （随侧边栏切换，显示对应面板）        │
│          │                                     │
│          │                                     │
├──────────┴─────────────────────────────────────┤
│  底部状态栏（harness 状态 · GitHub 状态 · 版本）  │
└────────────────────────────────────────────────┘
\`\`\`

- **标题栏**：原生标题栏（最稳、少 bug），含最小化 / 最大化 / 关闭。
- **侧边栏**：约 200px，**可折叠**（收起后只剩图标）。
- **内容区**：承载「聊天 / GitHub / 设置」三个面板。
- **底部状态栏**：常驻显示 harness 连接状态、GitHub 登录状态、版本号。

### 3.2 侧边栏

\`\`\`
┌──────────┐
│  💬 聊天   │   ← 默认选中
│  🐙 GitHub │
│  ⚙️ 设置   │
│          │
│  ─────── │
│  ● harness│   ← 状态点：绿=就绪 / 黄=启动中 / 红=出错
│  v1.0.0  │
└──────────┘
\`\`\`

- 三个主入口：聊天、GitHub、设置。
- 底部固定区：harness 状态灯 + 版本号；点击状态灯弹出 harness 快捷菜单（启动/停止/重启/在浏览器打开/复制地址/查看日志）。

### 3.3 导航关系

- 一级导航：侧边栏在三个面板之间切换。
- 二级导航（GitHub 面板内部）：面包屑 + 返回按钮（如「我的仓库 / 仓库名 / Issues」）。

---

## 4. 每个模块的用途

### 4.1 侧边栏导航
用途：全局一级导航，在「聊天 / GitHub / 设置」间切换；底部承载 harness 状态灯与版本号。

### 4.2 聊天面板（内嵌 DeepSeek Harness + harness 运行控制台）
用途：主聊天入口。内嵌 harness 完整 UI 用于对话；上方加一条 **harness 运行控制台**，用于呈现 harness 网页 UI **没有体现**的部分。

> **核心原则（不换皮）**：app 不做 harness 已有的界面（对话、模型选择、harness 自带设置等）的翻版；只补充 harness 网页里**看不到**的东西：
> 1. **后端进程日志**：npx @deepseek-ai/dsh web 进程在终端打印的 stdout/stderr（启动信息、监听端口、模型连接、错误堆栈等）——这些原本只在 PowerShell 终端里，网页 UI 看不到。
> 2. **进程生命周期信息与控制**：PID、运行时长、由谁启动、启动/停止/重启、在默认浏览器打开、复制地址。
> 3. **端口复用检测信息**：提示「检测到已有 harness 并复用」或「由本 app 启动」。

### 4.3 GitHub 面板（只读）
用途：只读浏览 GitHub。仓库列表、文件、issue、PR、commit、README、搜索。**不提供任何写操作**。

### 4.4 设置面板
用途：管理 app 与 harness 的配置：主题、启动行为、GitHub token、harness 端口、数据备份/恢复。

### 4.5 系统托盘
用途：后台常驻入口。隐藏后从托盘唤起，或直达某个面板；托盘「退出」才真正停止 harness 并退出。

### 4.6 底部状态栏
用途：全局展示 harness 连接状态与 GitHub 登录状态，不随面板切换而消失。

---

## 5. 每个模块的最必要数据和操作

### 5.1 全局框架
- 数据：当前选中面板、侧边栏折叠状态、窗口大小与位置、主题。
- 操作：切换面板、折叠/展开侧边栏、最小化/最大化/关闭（关闭=隐藏到托盘）。

### 5.2 聊天面板 + harness 运行控制台
- 数据：harness 进程状态（未启动/启动中/运行中/出错）、PID、启动时间、端口、后端日志缓冲（stdout/stderr）。
- 操作：加载内嵌 harness 页面；查看后端日志；启动/停止/重启 harness；在浏览器打开；复制地址。

### 5.3 GitHub 面板
- 数据：token（加密存储）、登录用户、仓库列表（名称/描述/star/fork/语言/更新时间）、仓库详情（文件树、文件内容、issue、PR、commit、README）。
- 操作：配置/验证/清除 token；登录/登出；查看「我的仓库 / 星标 / 搜索」；进入仓库详情；切换子标签（文件/Issues/PR/Commits/README）；查看条目详情；返回上一级。

### 5.4 设置面板
- 数据：主题、启动行为、窗口偏好、GitHub token、harness 端口、DSH_HOME（只读展示）。
- 操作：切换主题、改启动行为、保存/验证/清除 token、启动/停止/重启 harness、查看日志、备份、恢复。

### 5.5 系统托盘
- 数据：当前运行状态。
- 操作：显示主窗口；直达某面板；真正退出（停止 app 启动的 harness 后退出）。

---

## 6. 首页与各模块之间的关系

- **首页 = 聊天面板（内嵌 harness）**，是 app 启动后的默认落地页，也是核心价值所在。
- 聊天面板是中心：其余模块（GitHub、设置）通过侧边栏从首页进入，使用后仍可切回聊天。
- 设置影响全局：主题、harness 生命周期、GitHub token 都来自设置。
- GitHub 依赖设置：GitHub 面板需要设置里的 token 才能访问。
- harness 状态全局可见：在侧边栏底部状态灯、聊天面板控制台、底部状态栏、托盘菜单中一致呈现。
- 关系图：

\`\`\`
        ┌───────────────────────┐
        │      首页（聊天）       │ ← 默认落地页
        └──────────┬────────────┘
        ┌──────────┼──────────────┐
        ▼          ▼              ▼
     GitHub      设置          （返回聊天）
        ▲          │
        └──token───┘  主题/生命周期/端口 影响全局
\`\`\`

---

## 7. 本地数据文件、备份和恢复要求

### 7.1 数据分为两类

**A. harness 自有数据（不重写其机制，只负责纳入备份）**
- 位置：C:\Users\Mao\.dsh
- 内容：settings.yaml、.credentials.yaml、sessions/（聊天历史，每会话一个 session.jsonl.zstd）、profiles/、storages/、.anonymous-user-id。

**B. app 自有数据（app 负责创建与维护）**
- 位置：Electron userData，即 %APPDATA%\<AppName>\
- config.json：窗口大小/位置、主题、启动行为、侧边栏折叠状态。
- github.json：GitHub token（用 Electron safeStorage 加密）+ 登录用户名。
- harness.log：捕获到的 harness 后端日志（做轮转，保留最近 N 个，单个上限如 1MB）。

### 7.2 持久化铁律

- **绝不**把任何用户数据放进：npx 临时缓存、系统 temp 目录、任何易被清理的目录。
- app 启动 harness 时**必须**沿用 DSH_HOME = C:\Users\Mao\.dsh，保证聊天历史/设置不丢。

### 7.3 备份与恢复

- 设置面板提供「一键备份」：把「app 数据目录 + ~\.dsh」打包为 zip，存到用户指定位置（含时间戳）。
- 提供「从备份恢复」：解包 zip，把两类数据还原到对应目录（恢复前自动先备份当前数据，防止覆盖丢数据）。
- 日志做滚动轮转，避免无限膨胀。

---

## 8. 第一版必须完成的功能

1. Electron 桌面壳，打包成 Windows 可执行程序，双击即用。
2. 原生标题栏 + 可折叠侧边栏（聊天 / GitHub / 设置）+ 底部状态栏。
3. 无缝启动 harness：app 启动时后台运行 npx @deepseek-ai/dsh web，内嵌 http://127.0.0.1:3080 到「聊天」面板，并沿用 DSH_HOME=~\.dsh。
4. 端口复用：检测到 3080 已有 harness 时复用、不重复启动，并明确提示。
5. harness 运行控制台（不换皮）：后端日志（stdout/stderr）+ 进程状态（PID/时长/端口/来源）+ 启动/停止/重启/浏览器打开/复制地址。
6. GitHub 只读：配置只读 PAT（加密保存）、验证、我的仓库/星标/搜索、仓库详情（文件/Issues/PR/Commits/README）、面包屑+返回。
7. 设置面板：主题、启动行为、窗口偏好、GitHub token 管理、harness 端口、DSH_HOME 只读展示。
8. 数据持久化：config.json、加密 github.json、harness.log 轮转；沿用 ~\.dsh。
9. 一键备份 / 恢复。
10. 系统托盘常驻：关闭=隐藏到托盘；托盘可显示窗口/直达面板/真正退出。
11. 明暗主题（浅色/深色/跟随系统）。
12. 健壮性：启动失败可重试、可看日志；退出时精准停止 app 启动的 harness 进程。

---

## 9. 第一版暂时不做的功能

1. 不另做接官方 API 的自定义聊天界面（只内嵌 harness）。
2. GitHub 不做任何写操作（不创建/评论 issue、不建 PR、不 push）。
3. 不做 Git 客户端（不 clone/pull/push 本地仓库）。
4. 不修改 harness 本身源码（故 v1 不安装 pnpm）。
5. 不做全局快捷键、开机自启动。
6. 不做自定义细标题栏（用原生标题栏）。
7. 不做多用户、云同步、多端。
8. 不强行改 harness 内嵌界面的主题/外观（避免 bug，主题只作用于 app 自绘部分）。

---

## 10. 可以实际检查的验收标准

1. **一键启动**：双击 app，不打开 PowerShell、不手动开浏览器，约 30 秒内出现聊天界面且能正常对话。
2. **无重复进程**：在已有 harness 运行时启动 app，不重复启动、自动复用并提示。
3. **关闭/托盘**：点关闭 = 隐藏到托盘，harness 继续运行；托盘「退出」= 停止 app 启动的 harness 进程后退出，无残留进程。
4. **错误可恢复**：harness 启动失败时显示红色状态 + 可「重试 / 查看日志」，不白屏卡死。
5. **设置持久**：重启 app 后，主题、窗口大小位置、侧边栏折叠状态、GitHub 登录态保持不变。
6. **聊天历史持久**：重启 app / 重启电脑后，聊天历史仍在（~\.dsh 数据未被改动）。
7. **GitHub 只读**：能浏览仓库/文件/issue/PR/commit/搜索；且全程不调用任何写接口。
8. **主题切换**：明暗主题切换即时生效。
9. **后端日志可见**：控制台能看到 harness 进程的 stdout/stderr（harness 网页 UI 看不到的那部分）。
10. **备份/恢复**：一键备份后，删改数据再恢复，设置与聊天历史能还原。
11. **内嵌无 bug**：harness 页面正常加载、无白屏，websocket/交互正常；出现异常时可用「在浏览器打开」兜底。
