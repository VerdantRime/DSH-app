# 验收清单（对应 PRD.md §10）

> 生成时间：全部开发阶段（P0–P9）完成后。测试：12 个文件 / 36 个用例全部通过；类型检查通过；构建通过；打包产物已生成并通过冒烟。

## 关键产物位置

- 源码根目录：C:\Users\Mao\Desktop\qwq临时\deep seek app\
- 安装包（NSIS）：release\DSH Workdesk Setup 1.0.0.exe
- 便携版（portable）：release\DSHWorkdesk-1.0.0-portable.exe
- 解包可执行：release\win-unpacked\DSH Workdesk.exe
- 应用数据目录：%APPDATA%\dsh-workdesk\（config.json 等）
- harness 数据目录：C:\Users\Mao\.dsh（沿用，未改动）

## 逐条验收

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 一键启动，无需 PowerShell/浏览器 | ✅ 通过 | 打包后进程启动成功（冒烟 ALIVE）；启动时自动运行 harness 并复用已有实例 |
| 2 | 无重复进程（端口复用） | ✅ 通过 | harness-manager 复用测试：端口占用→reused、source=external、pid=null |
| 3 | 关闭=隐藏到托盘；托盘退出=停止自身 harness | ✅ 通过 | stop() 只杀 ownPid、reuse 不杀（测试覆盖）；托盘图标已改为可见占位图标 |
| 4 | 错误可恢复（红色状态 + 重试/日志） | ✅ 实现 | deriveControls 测试：error 态可 start；控制台提供重试/日志 |
| 5 | 设置持久（主题/窗口/折叠/GitHub 登录态） | ✅ 通过 | store 跨实例持久化测试；打包冒烟确认 %APPDATA%\dsh-workdesk\config.json 生成 |
| 6 | 聊天历史持久 | ✅ 实现 | spawn 显式传 DSH_HOME=~\.dsh；备份包含 ~\.dsh |
| 7 | GitHub 只读（能看、无写接口） | ✅ 通过 | 仅用 list/get/search 只读端点；mapper 与客户端测试覆盖；代码评审无写接口 |
| 8 | 主题切换即时生效 | ✅ 通过 | theme 纯函数测试（system/light/dark 解析与循环） |
| 9 | 后端日志可见（stdout/stderr） | ✅ 通过 | log 捕获（含 env 透传）测试；聊天面板日志抽屉展示 |
| 10 | 一键备份 / 恢复 | ✅ 通过 | backup 打包-恢复往返测试 + service 测试（恢复前自动备份） |
| 11 | 内嵌 harness 无 bug | ✅ 实现 | 构建通过；webview 使用 persist 分区（冒烟确认 Partitions 目录生成）、禁 will-navigate、外链走系统浏览器 |

## 需要你在 GUI 上做最终确认的项（无法用自动化完全替代）

1. 内嵌 harness 的实际对话体验（依赖真实 harness，已确认能复用连接）。
2. GitHub 面板：填入你的只读 Personal Access Token 后验证仓库列表 / 文件 / Issue / PR / Commit / README。
3. 明暗主题、托盘图标、窗口布局与折叠的视觉呈现。
4. 双击 portable exe 的最终手感（我做过 win-unpacked 冒烟，portable 由 electron-builder 生成）。

## 测试统计

- Test Files：12 passed（smoke、panels、store、harness-manager、harness-ui、credentials、logger、backup、backup-service、github、github-service、theme）
- Tests：36 passed
- typecheck：通过
- build（electron-vite）：通过
- 打包（electron-builder）：NSIS + portable 均生成成功
